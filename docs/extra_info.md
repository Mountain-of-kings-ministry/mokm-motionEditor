engines=['cpu','gl'] → CPU + OpenGL support (very useful)
simd=true → Important for performance
threads=true → Multi-threading
partial=true → Critical for low-end responsiveness
loaders → Includes images + Lottie (needed for motion graphics)
log=false → Less overhead
extra=['lottie_exp'] → Lottie Expressions support
